# SAM-Q-VLA-HMVP Requirements

# PyTorch CUDA 13.2 — install from PyTorch's own wheel index
--extra-index-url https://download.pytorch.org/whl/cu124

# Core
torch>=2.0.0
torchvision>=0.15.0

# Transformers
transformers>=4.37.0
qwen-vl-utils>=0.0.1

# Image processing
Pillow>=10.0.0
opencv-python>=4.8.0,<4.13.0

# Utilities
numpy>=1.26.0,<2.0.0
tqdm>=4.65.0
pyyaml>=6.0
einops>=0.7.0
triton>=3.0.0
pycocotools>=2.0.0

# Attention
flash-attn>=2.5.0

# Training
accelerate>=0.25.0
peft>=0.7.0
trl>=0.8.0  # SFTTrainer for LLM fine-tuning

# Optional: for visualization
matplotlib>=3.7.0